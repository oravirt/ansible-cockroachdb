---

  - name: Generic host setup
    hosts: "{{ hostgroup }}"
    become: true
    roles:
      - host-setup


  - name: Install CockroachDB
    hosts: "{{ hostgroup }}"
    become: true
    roles:
      - cockroach-install
      - cockroach-cluster
      - cockroach-db

